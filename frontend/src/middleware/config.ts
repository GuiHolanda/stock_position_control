import { AuthorizationMiddleware } from "./authorizationMiddleware";

export const middleware = [
  // Your middleware functions exported from different middleware files
  AuthorizationMiddleware,
];
